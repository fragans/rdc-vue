<template>
  <router-link to="/">
    <div class="max-w-sm rounded overflow-hidden shadow-lg">
      <div class="w-full bg-cover bg-center" :style="thumbnail" :alt="item.title" />
      <div class="px-6 py-4">
        <div class="font-bold text-xl mb-2">{{ item.title }}</div>
        <p class="text-gray-700 text-base">
          {{ item.desc }}
        </p>
      </div>
      <div class="px-6 pt-4 pb-2">
        <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2">{{ item.dateStart }}</span>
      </div>
    </div>
  </router-link>
</template>
<script>
export default {
  name: 'CardEvent',
  props: ['item'],
  computed: {
    thumbnail () {
      return {
        backgroundImage: `url(${this.item.thumbnail})`,
        paddingBottom: '56.25%'
      }
    }
  }
}
</script>